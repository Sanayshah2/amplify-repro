{
  "name": "amplify-repro",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "amplify:S3Trigger332680be": "cd amplify/backend/function/S3Trigger332680be/lib && mv lib/ ../src/ && mv bin/ ../src && npm ci && npx tsc -p ../tsconfig.json && rm -rf ../src/node_modules/ && npm prune --production && mv node_modules/ ../src/ && npm ci && cd -",
    "build": "mkdir -p dist && cp index.html dist/"
  },
  "dependencies": {},
  "devDependencies": {
    "@aws-amplify/cli": "^12.10.1",
    "typescript": "^5.2.2"
  }
}
